#! /bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin
DAEMON=/usr/sbin/nullmailer-send
NAME=nullmailer
DESC=mail-transfer-agent
DAEMON_OPTS=-d

test -x $DAEMON || exit 0

. /lib/lsb/init-functions

set -e

case "$1" in
  start)
	log_begin_msg "Starting $DESC:" "$NAME"
    if [ ! -c /var/spool/nullmailer/trigger ]; then                                                                                                        
            rm -f /var/spool/nullmailer/trigger                                                                                                            
            mkfifo /var/spool/nullmailer/trigger                                                                                                           
    fi                                                                                                                                                     
	chown mail:root /var/spool/nullmailer/trigger
	chmod 0622 /var/spool/nullmailer/trigger
	start-stop-daemon --start --quiet --chuid mail --exec "$DAEMON" --oknodo -- $DAEMON_OPTS
	log_end_msg $?
	;;
  stop)
	log_begin_msg "Stopping $DESC:" "$NAME"
	start-stop-daemon --stop --quiet --user mail --exec "$DAEMON" --oknodo
	log_end_msg $?
	;;
  restart|reload|force-reload)
	$0 stop
	sleep 1
	$0 start
	;;
  *)
	N=/etc/init.d/$NAME
	echo "Usage: $N {start|stop|restart|reload|force-reload}" >&2
	exit 1
	;;
esac

exit 0
